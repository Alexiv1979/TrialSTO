
&НаКлиенте
Процедура ПриОткрытии(Отказ)    
	
	Элементы.ДекорацияОбъектОтбора.Заголовок = Строка(ОбъектОтбора);
		
КонецПроцедуры   

&НаКлиенте
Процедура ПриЗакрытии(ЗавершениеРаботы)    
		
	СтруктураДанных = Новый Структура("ФормаРедактирования, ПричинаОбращения", "РедактированиеПричиныОбращения", ПричинаОбращения);
	
	ОповеститьОВыборе(СтруктураДанных);   		

КонецПроцедуры

